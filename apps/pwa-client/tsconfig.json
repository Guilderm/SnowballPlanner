{
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    "baseUrl": ".",
    "paths": {
      "@shared/*": ["../../apps/shared/*"]
    }
  },
  "include": [
    "utilities/**/*.ts"
    // Add other relevant include paths as needed
  ],
  "exclude": ["node_modules", "dist", "test", "**/*spec.ts"]
}
